#include<bits/stdc++.h>
using namespace std;
int find(int arr[],int n,int tar){
    int i=0;
    int chota=arr[i]/10;
    in
}
int main(){
    int n;
    cin>>n;
    int tar;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    find(arr,n,tar);
}