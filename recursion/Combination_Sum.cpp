#include<bits/stdc++.h>
using namespace std;

vector<vector<int>> combinationSum(vector<int>& candidates, int target) {
    vector<int>sum;
    helper(candidates,tar,sum)
        
}
int main()
{
    vector<int>s;
    cin>>n;
    int tar;
    cin>>tar;
    combinationSum(n,tar)

}